package Section3;

public class KeywordsExpressions {
    public static void main(String[] args) {
        //keywords are blue or orange here
        int int2 = 5; // cant have exact word "int"
        // true, false, and null are not keywords, but they are literals and reserved words that cannot be used as identifiers.
        double mile = 1.609344d;
        double kilometers = (100 * mile);
        int highScore = 50; // expression highScore = 50
        if (highScore == 50) { //expresson highScore == 50
            System.out.println("This is an expression");// expression "This is an expression"
        }


    }
}
